dist/
coverage/
node_modules/
docs-site/
pnpm-lock.yaml

# Vendored upstream inputs are kept byte-identical to their source so the
# provenance diff stays meaningful — never reformat them.
sources/

# Generated artifacts (emitted by scripts/build-data.mjs, not hand-edited).
#
# Deliberately unformatted: `data:check` proves the committed output is
# byte-identical to a fresh build, so a formatter rewriting it afterwards would
# put the two gates permanently at odds. The generator owns their layout;
# `typecheck` still covers them, which is the part that catches real defects.
data/
src/generated/

# Differential fixtures are verbatim transcriptions of another project's
# dictionary, produced by scripts/differential/extract-dictionary.mjs. They are
# stored on one line on purpose: pretty-printing 4,898 packed tuples turns a
# 251 KB file into megabytes of diff nobody reads.
tests/differential/fixtures/
